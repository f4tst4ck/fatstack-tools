{
  "name": "fatstack-tools",
  "private": true,
  "license": "MIT",
  "description": "Open components of Fatstack: the x402 SDK, the AI SDK integration, and the USDC payment splitter.",
  "engines": { "node": ">=22" },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "contracts:test": "cd contracts && forge test"
  }
}
